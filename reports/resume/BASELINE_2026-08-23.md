# Fullstack2026 resume baseline

> Status: **VERIFIED pre-change baseline**
> Captured: `2026-08-24T09:37:21+03:00` to `2026-08-24T09:38:24+03:00`
> Time zone: `Asia/Jerusalem` (`Israel Standard Time`)
> Baseline commit: `20f27a7a5fdd0d68a85f892438849d51d6ff82ba`

## Repository identity

| Fact | Evidence |
|---|---|
| Remote | `https://github.com/LiriothTeltanion/Fullstack2026.git` |
| Initial branch | `main` tracking `origin/main` |
| Initial worktree | Clean (`git status --short` produced no paths) |
| Working branch created after baseline | `chore/resume-foundation-2026-08-23` |
| HEAD | `20f27a7` — `fix(security): publish dependency patch v1.1.1` |
| Tag | `v1.1.1` |
| Tracked blobs | 831 (`git ls-files`) |
| Git path behavior | `core.ignorecase=true` |
| Actual clone | `.../040_LEARNING_ACADEMY/_042_Fullstack2026` |
| Adjacent selected folder | `.../040_LEARNING_ACADEMY/042_Fullstack2026` contains only `README_NOVA.md` and is not a Git repository |

No remote write, fetch, merge, PR mutation, deployment, or branch deletion occurred while capturing this baseline.

## Tool versions

| Tool | Version / availability |
|---|---|
| Git | Repository commands available; exact Git client version was not used as a quality claim |
| Node.js | `v26.3.1` |
| npm | `11.19.0` |
| Python | `3.14.6` |
| GitHub CLI | `2.96.0` |
| PostgreSQL client | `psql` unavailable in `PATH` |
| Ruff | Unavailable in `PATH` |

The CI workflow currently targets Node.js 24 and Python 3.13, so local runtime success does not by itself prove CI parity.

## Root scripts at baseline

| Script | Command | Actual scope |
|---|---|---|
| `lint` | `eslint "Week*/**/*.{js,ts}"` | Intended JS/TS lint; baseline glob resolves to no files in this environment |
| `format:check` | `prettier --check "Week*/**/*.{js,ts}"` | Formatting for matched JS/TS files |
| `test` | `npm run test:js && npm run test:python` | Three Node test modules and five Python test modules |
| `quality` | `python tools/nova_quality_gate.py --repo . --strict` | Static syntax/security/link/repository heuristics; writes NOVA reports unless `--no-write` is supplied |
| `syntax` | `python tools/nova_quality_gate.py --repo . --syntax-only --strict` | Static parsing/transpilation, not behavior or semantic TypeScript validation |
| `audit` | `python tools/nova_ultimate.py --repo . --audit --no-open` | Regenerates selected NOVA reports |
| `readme:generate` | NOVA README forge | Broad generated documentation mutation; intentionally not run in Wave 1 |

## Mandatory baseline commands

| Command | Exit | Result | Evidence / limitation |
|---|---:|---|---|
| `npm ci` | 0 | PASS | Added 127 packages and audited 128. Reported two high-severity development-tool vulnerabilities. |
| `npm run format:check` | 0 | PASS | Prettier reported all matched JS/TS files formatted. It does not prove runtime behavior. |
| `npm run lint` | 2 | FAIL | ESLint 8.57.1 reported `No files matching the pattern`. No curriculum file was linted by the script. |
| `python tools/nova_quality_gate.py --repo . --strict --no-write` | 0 | PASS | `errors=0 warnings=0 files=1018`. Static gate only; reports were not rewritten. |
| `npm test` | 0 | PASS | Node: 4 cases passed. Python: 12 cases passed. These are representative anchors, not curriculum-wide validation. |
| `git diff --check` | 0 | PASS | No whitespace errors in the pre-change diff; there was no tracked diff. |

Additional diagnostics:

- Explicit enumeration of the 73 tracked/visible JavaScript and TypeScript sources made ESLint run and exposed **65 existing errors**. These are not introduced by this wave.
- `npm audit --omit=dev` passed with zero production vulnerabilities.
- Full `npm audit --audit-level=high` failed with two high-severity development dependency advisories (`brace-expansion` and `js-yaml`).
- Strict semantic TypeScript for Union Type Validator failed with `TS2688` because `@types/node` is declared in the nested package but is not installed by the root lockfile.
- `psql` is unavailable, so no PostgreSQL execution was claimed.

## What the current quality reports prove

| Claim | What is actually proven | What is not proven |
|---|---|---|
| `PASS`, 0 errors, 0 warnings | The current working tree passed NOVA's implemented static rules on this machine. | Browser behavior, interactive CLI flows, SQL execution, external APIs, accessibility, semantic TypeScript, ESLint, mastery, or assignment correctness. |
| 1,018 files scanned | NOVA scanned 816 tracked non-report files plus 202 ignored local files (201 `desktop.ini` files and `README.NOVA-PREVIEW.md`). | It is not the tracked-file count and is not a stable curriculum metric. |
| 14 automated test files (historical report) | Historical NOVA discovery counted files under test-oriented paths. | It does not mean 14 executable suites. Current root runners execute 3 Node modules and 5 Python modules. |
| 92% readiness | A dated repository-maintainability heuristic from 2026-07-15. | It is not a course grade, completion percentage, learning state, or current release verdict. |
| TypeScript check | Files transpile without syntax diagnostics. | Cross-file resolution, declared dependencies, strict types, or `tsc --noEmit`. |

## Curriculum roots in the Git tree

The physical Windows tree displays six roots because case variants collapse. The Git tree contains seven exact top-level names:

1. `Week1Python`
2. `Week2OOP`
3. `Week3JavaScriptandDOM`
4. `Week4AdvAsynchronousJavaScript`
5. `Week5MiniProjectAndTypeScript`
6. `Week5MiniprojectAndTypeScript` — legacy case-conflicting root
7. `Week6DatabasesAndNodejs`

## Baseline blockers

1. Ten case-insensitive directory collision groups exist in the Git index, all under the two Week 4/Week 5 collision families.
2. The root lint script resolves no files; an explicit lint inventory has 65 pre-existing errors.
3. Two high-severity development dependency advisories remain; production dependency audit is clean.
4. Strict Union Type Validator compilation cannot resolve `@types/node` from the root install.
5. The Day 2 SQL answer selects `email`, while the Day 1 schema declares only `id`, `first_name`, and `last_name`; no local official assignment prompt establishes the intended answer.
6. Root and week documentation contains dated heuristics and unsupported completion/tooling claims. Full documentation repair belongs to Wave 2.
7. The generated NOVA catalogs contain stale pre-normalization paths.

## Learning boundary

This baseline infers **no learning mastery**. A file may be present, runnable, or covered by a representative automated check without proving that Kevin can explain or reproduce it. Repository evidence and learning evidence are tracked separately in `.learning/`.
